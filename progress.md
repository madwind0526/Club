# Progress Log

## 2026-08-06 — 초기 스캐폴딩

- SNS-Reader(같은 워크스페이스의 다른 프로젝트) 구조를 참고해 Electron + React + TypeScript + Vite로 새 프로젝트 시작
- `electron/main.ts` + `electron/preload.ts`: 회원/활동/게시판/설정 IPC 핸들러, Knox ID + SHA-256 비밀번호 로그인, 초기 관리자 계정 자동 시드
- `vite.config.mts`: Electron 없이 브라우저에서도 개발할 수 있도록 `/api/*` 미들웨어로 동일한 데이터 계층을 dev 서버에도 구현
- `src/data/*Store.ts`: `window.clubApp` IPC와 `/api/*` fetch를 모두 시도하는 이중 폴백 패턴
- 화면 구성: Home / Activities(Photo·Card·List) / Board / Members / ActivityRegister / ActivityReport / Settings / Profile / Login, 좌측 메뉴 + 상단 툴바 + 하단 시스템 메시지 레이아웃
- 디자인: SNS-Reader의 크림톤 대신 순수 그레이스케일 팔레트로 새로 정의 (라이트/다크 두 세트)
- 샘플 데이터(회원 8명, 활동 9건, 게시글 11건)로 초기 상태 구성

## 2026-08-06 밤 ~ 2026-08-07 — 기능 확장

- **로컬 파일 렌더링 문제 해결**: Electron 창이 `http://127.0.0.1:5173` 오리진으로 뜨기 때문에 `file://` 리소스가 차단되는 문제를 발견 → `club-media://` 커스텀 프로토콜을 등록해 클럽 로고/사진/영수증/경비 이미지를 안전하게 로드하도록 수정
- **활동 상세를 팝업으로 통합**: 별도 페이지였던 활동 상세/리포트 화면을 팝업(모달)으로 전환. Home과 Activities 어디서든 완료된 활동은 "상세 내역 확인", 예정된 활동은 "참가 신청"(누구나) + "활동등록"(admin)이 뜨도록 통일
- **ActivityReportView 대폭 강화**: 제목/활동 내용 직접 수정, 참석자 추가/삭제, 사진·영수증·경비 썸네일 개별 삭제(폴더 재스캔 시 병합만 하도록 변경), 썸네일 클릭 시 원본 크게보기 팝업, 활동 계획서 파일 첨부(수동 선택 + `Plan` 폴더 파일명 규칙 기반 자동 첨부)
- **주간 정리**: 전체 활동을 List View로 보여주고 클릭하면 바로 상세 리포트 팝업으로 이동 (admin 전용, 사이드바에서 비활성화 처리)
- **월간 정리**: 한 달의 여러 활동을 하나로 종합하는 리포트 신규 구현
  - 사진/영수증 사진/경비 사진을 주차별로 묶어서 순서대로 표시
  - 각 주에 입력했던 영수증/경비 표를 월별 표 하나로 합산
  - 참석자별로 1차/2차/3차... 출석 여부와 합계, 참석 횟수 기준 후원금액(1회 50,000원 / 2회 이상 100,000원) 자동 계산, 총원·총 후원금액 합계
- **회원 관리**: CSV 대신 JSON 파일로 불러오기/내보내기
- **UI 다듬기**: 완료/예정 상태 뱃지 색상 구분(초록/파랑), 홈 통계 카드 클릭 시 회원관리·Activities로 이동, 홈 통계 카드 가운데 정렬, 다크 모드에서 input/select/textarea가 라이트 스타일로 남아있던 문제 수정, 테마 선택을 드롭다운에서 라이트/다크 버튼 선택 방식으로 변경

## 2026-08-07 — 저장소 정리

- Git 저장소 초기화, `https://github.com/madwind0526/Club`에 푸시
- `data/runtime/`(샘플 회원·활동·게시판 데이터)과 `assets/`(클럽 로고)는 저장소에 포함, 개인 테스트용 `Input/` 폴더는 제외
- 로그인 화면에 노출되던 초기 관리자 비밀번호 힌트 문구 제거 (README에 문서화하는 것으로 대체)

## 2026-08-07 밤 ~ 2026-08-08 — 회원 불러오기/월간 Excel/프로필 편집

- **회원 불러오기/내보내기 전면 개편**
  - Settings에 회원 파일 형식(JSON ↔ 탭 구분 TXT)과 방식(추가/교체) 설정 추가
  - 회원 관리 버튼을 **불러오기**(파일 선택), **자동불러오기**(`assets/members.json` 또는 `assets/members.txt`를 바로 읽음), **내보내기** 3개로 분리 — 셋 다 Settings의 형식/방식을 따름
  - 추가 모드에서 Knox ID가 이미 있는 회원은 자동으로 건너뛰고 몇 명 제외됐는지 안내
- **활동 계획서 개선**: 지원 확장자에 gif·pdf 추가, 버튼+파일명 텍스트 대신 썸네일 타일로 표시(이미지는 실제 썸네일, 문서는 아이콘), 클릭 시 이미지·PDF는 팝업 미리보기·문서 파일은 OS 기본 프로그램으로 열기(`shell.openPath`)
- **월간 정리 Excel 내보내기** (`server/excelExport.ts`, ExcelJS) 신규 구현
  - Summary(2x2 표 + 참석자 표를 좌측에, 사진/영수증/경비를 우측으로 나란히 배치) + 활동이 있는 주차별 시트(계획서·참석자·사진·영수증·경비 그대로) + 영수증/경비 통합 시트 + 회원 목록 시트
  - 모든 표에 테두리 적용, 로컬 이미지를 실제 xlsx에 임베딩(`club-media://` 경로를 원본 파일 경로로 역변환해서 읽음)
  - Electron에서는 저장 위치를 묻는 네이티브 dialog, 브라우저 dev 모드에서는 일반 파일 다운로드로 동작
- **Profile 자체 편집**: 이름/부서/비고 수정 + 새 비밀번호 변경(확인 입력 포함), 저장 시 로그인 세션도 즉시 갱신
- **Home 행 레이아웃 조정**: 완료/예정 행사 목록에서 참석자 수를 오른쪽으로, 기존 참석자 표시 자리엔 주차 표시, 날짜·참석자 글자 크기 확대
- **저장소 정책 변경**: 개인 테스트용으로 제외했던 `Input/`(샘플 사진·영수증·경비·계획서)을 다시 저장소에 포함하기로 결정, `Report/`(Excel 내보내기 예시)와 `screenshot/`(앱 화면)도 함께 추가

## 2026-08-09 — 경로 이식성 수정 / Settings 확장 / 계획서 다중 첨부 / 월간 Excel 재구성

- **경로 이식성 버그 수정**: 클럽 로고·활동 계획서·Excel 이미지가 "내 PC에서는 보이는데 다른 PC에서는 안 보이는" 문제의 원인은 `data/runtime/app-settings.json`·`activities.json`에 커밋된 샘플 경로가 특정 개발 PC의 **절대 경로**(`C:\Claude\Club\...`)였기 때문 — 저장소를 다른 위치에 클론하면 그 경로가 존재하지 않음. `server/paths.ts`의 `resolveAppPath()`(상대 경로는 `process.cwd()` 기준으로 해석)를 `club-media://` 프로토콜 핸들러·`shell:openPath`·Excel 이미지 임베딩에 적용하고, 샘플 데이터를 `assets/Logo.png`·`Input/...` 같은 상대 경로로 재인코딩
- **Settings 확장**: Photos/Receipts/Expenses/Plan 폴더 개별 경로(비워두면 데이터 루트 폴더 하위 사용), 회원 자동불러오기 파일 경로, 1회/2회 이상 참석 활동비 금액(기본 5,000원/10,000원), 보고서 클럽 이름 필드 추가
- **회원 불러오기**: 파일 가져오기/자동불러오기에 "초기 비밀번호" 입력 추가(admin 포함 전체 적용, 비워두면 각자 Knox ID)
- **활동 계획서**: `planFilePath`(단일) → `planFilePaths`(배열)로 전환. 자동 첨부가 `YYYY-MM-WeekN` 접두사에 맞는 **모든** 파일을 한 번에 첨부(기존엔 첫 번째 파일만), 개별 삭제 가능, 썸네일 그리드로 표시
- **활동 등록 시 폴더 자동 생성**: 등록 즉시 해당 월/주차의 Photos/Receipts/Expenses/Plan 폴더를 생성(`media:ensureFolders`)
- **월간 정리 Excel 전면 재구성**: Summary/주차 시트 모두 사진→참여인원→영수증→경비 순서로 통일, Summary 참여인원 표는 전체 회원 표시(주차별 출석 O 표시), 큰 제목("[YY년 M월] {보고서 클럽 이름} 활동 보고")과 각 패널 제목·부제(활동 인원/활동 사진/활동 인원 보고/출석 명단/영수증 첨부/활동결과/경비 첨부 등), 활동비 합·행사 경비 신청/지출 금액 합을 1x2 표로 표 옆에 배치

## 남은 작업 후보

- 실제 배포용 패키징(electron-builder를 통한 설치 파일 생성)
- 초기 관리자 비밀번호 변경 강제/알림
- Excel 내보내기 대상 월 선택 UX(현재는 월간 정리 화면에서 연 월에 한해 내보내기)
