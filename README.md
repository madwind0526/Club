# Club Management

사내/사외 동호회 운영을 위한 Electron 데스크톱 앱입니다. 회원 관리, 활동 일정과 참석 체크, 게시판, 주간/월간 활동 정리(Excel 내보내기 포함)를 한 곳에서 처리합니다.

## 주요 기능

### Home
- 클럽 로고/소개, 총 회원수·올해 활동 수·참가 인원 통계 카드(클릭 시 회원 관리/Activities로 이동)
- 완료된 행사 / 예정된 행사 요약(각 최대 5건) — 날짜, 주차, 참석자 수 표시

### Activities
- Photo / Card / List 3가지 보기, 검색
- 완료(초록)·예정(파랑) 상태 뱃지
- 완료된 활동 클릭 → "상세 내역 확인" 팝업 / 예정된 활동 클릭 → "참가 신청"(누구나) · "활동등록"(admin) 팝업

### 활동 등록 & 상세 리포트 (팝업) — 새 활동 등록·전체 수정은 admin 전용, 일반 회원은 열람 + 본인 참석 여부만 가능
- 제목/날짜/주차, 활동 내용, 참석자 추가·삭제 (admin)
- **활동 계획서**: 여러 파일을 첨부할 수 있음 — 수동 첨부(추가) 또는 `Plan` 폴더에서 파일명 규칙에 맞는 **모든** 파일을 자동으로 찾아 추가, 썸네일(이미지/문서 아이콘)로 표시 — 클릭 시 이미지·PDF는 팝업 미리보기, 문서 파일(doc/xls/ppt)은 OS 기본 프로그램으로 열림, 개별 삭제 가능 (admin)
- 사진·통장·영수증·경비 사진을 폴더에서 불러오기(재스캔 시 기존 목록에 병합, 개별 삭제 가능) + 클릭 시 원본 크게보기 (불러오기/삭제는 admin, 보기는 회원 누구나) — 은행 통장 사본은 사진과 완전히 동일한 방식으로 동작하며 별도 항목 표(날짜/금액 등)는 없음
- 영수증/경비 내역 표(추가/삭제, admin)
- 일반 회원은 완료된 활동을 열면 위 내용을 전부 열람만 할 수 있고, "참가 신청/참가 취소" 버튼으로 본인의 참석 여부만 바꿀 수 있음
- 활동을 등록하면 해당 월/주차의 Photos·Receipts·Expenses(·Plan) 폴더가 자동으로 생성됨

### Board
- 공지/일반/요청/QnA, 공지 상단 고정(admin), 댓글과 대댓글

### 회원 관리 (열람은 로그인한 회원 누구나, 추가·수정·삭제·가져오기는 admin 전용)
- 회원 추가/수정/삭제, 추가 시 초기 비밀번호 지정 가능(비워두면 Knox ID)
- **불러오기**: 파일 선택 창에서 직접 선택 / **자동불러오기**: Settings에 지정한 회원 파일(비워두면 `assets\members.json` 또는 `assets\members.txt`)을 자동으로 읽음 / **내보내기**
- Settings에서 형식(JSON ↔ 탭으로 구분된 TXT)과 방식(추가/교체)을 선택하면 세 버튼 모두 그 설정을 따름
- 불러오기/자동불러오기 시 "초기 비밀번호"를 입력하면 admin 포함 모든 가져온 회원에게 그 비밀번호가 설정됨(비워두면 각자 Knox ID)
- 추가 시 Knox ID가 이미 있으면 자동으로 건너뜀

### 주간 정리 — admin 전용
전체 활동을 List View로 모아보고 클릭 시 바로 상세 리포트 팝업으로 이동합니다.

### 월간 정리
한 달의 모든 활동을 하나로 종합합니다.
- 전체 회원의 1차·2차·... 출석 현황(O 표시) + 참석 횟수에 따른 활동비(1회/2회 이상 금액은 Settings에서 설정, 기본 5,000원/10,000원) 자동 계산
- 사진 / 통장 / 영수증 사진·내역 / 경비 사진·내역을 주차별로 모아서 표시 (통장은 사진과 동일한 스타일)
- **Excel로 내보내기 (admin 전용)** — 클릭하면 저장할 폴더를 고르는 **내장 파일 탐색기**가 뜹니다(바탕화면·내 문서·다운로드·프로젝트 폴더·프로젝트\Report 폴더 바로가기 제공, 하위 폴더 탐색 가능). 폴더를 선택하면 `club-management-YYYY-MM-monthly-report.xlsx` 파일이 그 폴더에 바로 저장됩니다 — **Electron 앱과 `npm run dev` 브라우저 모드 모두 동일하게 동작**하며, 둘 다 서버(=지금 이 앱/dev 서버를 실행 중인 PC)의 디스크에 저장됩니다. `.xlsx`에는 아래 시트가 생성됩니다 (모든 표에 테두리 적용)
  - **Summary**: 큰 제목("[YY년 M월] {보고서 클럽 이름} 활동 보고", Settings에서 클럽 이름 설정) 아래 사진 / 참여인원(전체 회원 출석표 + "활동비 합" 1x2) / 영수증(사진+"행사 경비 신청 금액 합" 1x2+내역 표) / 경비(사진+"행사 경비 지출 금액 합" 1x2+내역 표) 순서로 나란히 배치
  - **N주차** (활동이 있는 주만): Summary와 동일한 순서·형식으로 활동별 패널 구성(참여인원은 그 활동의 실제 참석자만 표시), 위쪽에 제목·활동 계획서·날짜·내용·참석 인원 정리
  - **통장 현황**: 큰 제목 아래 그 달의 통장 사본 사진을 주차별로 나열 (별도 표 없음)
  - **영수증** / **경비**: 월 전체 사진(주차별)과 통합 내역 표
  - **회원 목록**: 전체 회원 표
  - 내보내기가 끝나면 "DB 백업"(아래 참고)이 화면 표시 없이 자동으로 한 번 더 실행됩니다 (백그라운드)

### 로그인 / Profile
- Knox ID + 비밀번호, 회원 등급(회장/총무/감사/정회원)과 역할(admin/일반) 기반 권한
- Profile 화면에서 본인 이름/부서/비고 수정 및 비밀번호 변경 가능 (역할/등급/Knox ID는 본인 스스로 변경 불가 — admin만 변경 가능)

### Settings — admin 전용
- 클럽 이름/로고/소개, 데이터 루트 폴더(+ Photos/Bank/Receipts/Expenses/Plan 개별 폴더 경로 재정의, 비워두면 데이터 루트 폴더 하위 사용)
- 보고서 클럽 이름(월간 정리 Excel 제목에 사용), 1회/2회 이상 참석 활동비 금액
- 회원 불러오기 형식/방식, 회원 자동불러오기 파일 경로(비워두면 assets\members.json·txt)
- 테마(라이트/다크, 버튼 클릭 시 즉시 적용) — 여기서 바꾸는 건 로그인 화면을 포함한 앱 전체의 **기본값**. 각 회원은 상단 툴바의 해/달 아이콘(Profile 아이콘 옆)으로 admin 권한 없이도 자신의 브라우저에서만 적용되는 라이트/다크를 따로 override할 수 있음(`localStorage`에 저장 — 서버에는 저장되지 않음)

### DB 백업 / DB 복원 — admin 전용

좌측 사이드바의 "DB 백업" / "DB 복원" 메뉴(회원 관리 바로 위)에서 앱의 모든 데이터를 zip 하나로 백업하고, 필요할 때 그 시점 상태로 통째로 되돌릴 수 있습니다.

- **DB 백업**: 별도 화면 이동 없이 클릭하는 즉시 아래 내용을 zip 하나로 묶어 `Club\backup\YYYY-MM-DD-DB backup.zip`으로 저장합니다 (같은 날 다시 백업하면 그날 파일을 덮어씀).
  - `data/runtime/`의 4개 파일 — `activities.json`(활동), `members.json`(회원), `board.json`(게시판), `app-settings.json`(설정)
  - Settings의 "데이터 루트 폴더" 전체 (Photos/Bank/Receipts/Expenses/Plan) — 각 카테고리를 데이터 루트 폴더 **밖의** 별도 경로로 재정의한 경우 그 폴더는 백업 대상에서 제외됩니다
  - `Club\Report` 폴더 전체 (월간 정리로 내보낸 Excel 리포트들)
- **DB 복원**: 백업 목록(파일명·크기·생성 시각)에서 하나를 골라 "복원"을 누르면 확인 절차 후 — **현재 저장된 위 세 가지를 전부 삭제하고** 선택한 백업 안의 내용으로 그대로 교체합니다. 되돌릴 수 없는 작업이므로, 복원 전에 "DB 백업"으로 현재 상태를 한 번 더 저장해 두는 것을 권장합니다. 복원이 끝나면 앱이 자동으로 새로고침됩니다.
- **자동 백업**: 아래 두 시점마다 위와 동일한 백업이 화면 표시 없이 백그라운드로 자동 실행됩니다(실패해도 원래 동작 결과에는 영향 없음) — 데이터가 "정리된 상태"에 도달하는 시점마다 안전망을 자동으로 남겨두기 위함입니다.
  - 월간 정리에서 "Excel로 내보내기"가 성공했을 때
  - **admin**이 활동 상세 리포트(주간 정리 상세보기 등)에서 제목/내용/계획서/사진/통장/영수증/경비를 수정하고 "저장"을 눌렀을 때 — 일반 회원의 본인 참가 신청/취소 저장은 대상이 아닙니다(같은 저장 버튼이지만 너무 잦아 admin의 내용 수정 저장으로만 한정)
- 백업/복원 모두 **Electron 앱과 `npm run dev` 브라우저 모드 양쪽에서 동일하게 동작**하며, 둘 다 서버(=이 앱/dev 서버를 실행 중인 PC)의 디스크를 기준으로 합니다.

## 권한

| 기능 | 로그인 안 함 | 일반 회원 | admin |
|---|---|---|---|
| 데이터 열람 (활동/회원/게시판) | ❌ | ✅ | ✅ |
| 활동 참가 신청/취소 (본인 참석 여부만) | ❌ | ✅ | ✅ |
| 완료된 활동 상세 내역 열람 (제목/내용/참석자/계획서/사진/영수증/경비) | ❌ | ✅ | ✅ |
| 활동 상세의 제목/내용/계획서/사진/영수증/경비 수정, 다른 사람의 참석 여부 변경 | ❌ | ❌ | ✅ |
| 게시판 글 작성 | ❌ | ✅ | ✅ |
| 활동 등록/삭제 | ❌ | ❌ | ✅ |
| 월간 정리 Excel 내보내기 | ❌ | ❌ | ✅ |
| 회원 관리 (추가/수정/삭제/가져오기) | ❌ | ❌ | ✅ |
| 본인 프로필 수정 (Profile) | ❌ | ✅ | ✅ |
| Settings | ❌ | ❌ | ✅ |
| DB 백업 / DB 복원 | ❌ | ❌ | ✅ |

- 위 권한은 화면(UI)에서만이 아니라 **서버(백엔드) 쪽에서도 강제**됩니다 — Electron IPC 핸들러(`electron/main.ts`)와 `npm run dev`의 `/api/*`(`vite.config.mts`) 양쪽 모두 로그인 세션과 역할(role)을 직접 검사합니다. 브라우저 개발자 도구로 UI를 우회해 API를 직접 호출해도 로그인하지 않았거나 권한이 없으면 401/403으로 거부됩니다.
- `npm run dev`는 LAN(같은 네트워크)에서도 접속 가능하도록 `--host 0.0.0.0`로 열려 있으므로(주소창의 `http://<내부IP>:5173`), 이 서버 측 검사가 특히 중요합니다 — 로그인 세션은 httpOnly 쿠키로 관리되며, 로그인하지 않은 사용자는 어떤 데이터도 조회할 수 없습니다.
- 권한이 없는 메뉴/버튼을 시도하면 화면 하단 System 메시지에 경고("권한이 없습니다...")가 표시됩니다.
- **파일 관련 기능은 전부 "서버(=이 앱을 실행 중인 PC)의 디스크" 기준입니다** — 사진/통장/영수증/경비/계획서/로고 열람, 폴더에서 불러오기, Excel 저장 폴더 선택, 파일 열기(OS 기본 프로그램으로 열기), DB 백업/복원 전부 마찬가지입니다. 다른 PC에서 LAN으로 접속해 쓰더라도 항상 서버 PC의 파일을 보고, 서버 PC에 저장/복원합니다 — 접속한 PC 자신의 다운로드 폴더나 로컬 파일이 아닙니다.

## 기술 스택

- Electron + React + TypeScript + Vite
- 엑셀 생성: [ExcelJS](https://github.com/exceljs/exceljs)
- 데이터는 로컬 JSON 파일(`data/runtime/`)로 저장 — 별도 데이터베이스 없음
- 렌더러는 Electron IPC(`window.clubApp`)를 우선 사용하고, 브라우저 전용 `npm run dev` 모드에서는 Vite 미들웨어가 제공하는 `/api/*`로 동일하게 동작
- 로그인 세션과 역할(admin/일반) 검사는 두 백엔드(Electron 메인 프로세스, Vite 개발 서버) 모두에서 이루어짐 — 자세한 권한 범위는 아래 [권한](#권한) 참고
- 로컬 이미지/파일은 `club-media://` 커스텀 프로토콜을 통해 렌더러에 안전하게 전달됨 (자세한 내용은 [`CLAUDE.md`](CLAUDE.md) 참고)

## 시작하기

```bash
npm install
npm start        # Electron 앱 실행 (권장)
```

브라우저에서 UI만 확인하려면:

```bash
npm run dev       # http://127.0.0.1:5173
```

### Windows에서 `start.bat` / `stop.bat`

Windows에서는 탐색기에서 더블클릭으로 실행할 수 있는 `start.bat`/`stop.bat`도 제공됩니다.

- **`start.bat`**: 포트 5173에 이미 떠 있는 서버가 있으면(다른 프로젝트의 dev 서버가 남아있는 경우 포함) 먼저 종료한 뒤 `npm start`로 앱을 실행합니다.
- **`stop.bat`**: 포트 5173에 떠 있는 서버와 "Club Management" 창을 모두 종료합니다.

앱이 이미 실행 중인 상태에서 `start.bat`을 다시 실행하면 dev 서버는 재시작되지만 기존에 열려 있던 Electron 창은 자동으로 닫히지 않습니다 — 완전히 재시작하려면 `stop.bat`을 먼저 실행하세요.

### 최초 로그인

앱을 처음 실행하면 회원 데이터가 비어 있을 경우 관리자 계정이 자동 생성됩니다.

- Knox ID: `admin`
- 비밀번호: `admin1234`

로그인 후 Profile 또는 회원 관리에서 실제 정보로 수정하고 비밀번호를 변경하는 것을 권장합니다.

### 사진 / 통장 / 영수증 / 경비 / 계획서 폴더

Settings의 "데이터 루트 폴더"를 지정하면 아래 구조에서 자동으로 파일을 찾습니다. Photos/Bank/Receipts/Expenses/Plan은 Settings에서 개별 경로로 재정의할 수도 있습니다(비워두면 데이터 루트 폴더 하위 사용).

```
<데이터 루트 폴더>/
  Photos/YYYY-MM/WeekN/         예: Photos/2026-07/Week3/*.jpg
  Bank/YYYY-MM/WeekN/           은행 통장 사본
  Receipts/YYYY-MM/WeekN/
  Expenses/YYYY-MM/WeekN/
  Plan/YYYY-MM-WeekN.<확장자>    예: Plan/2026-07-Week3.jpg, 2026-07-Week3_사진.png
```

- Photos/Bank/Receipts/Expenses는 해당 폴더 안의 모든 이미지 파일(jpg/jpeg/png/gif/webp/bmp)을 불러옵니다.
- Plan은 파일명이 `YYYY-MM-WeekN`으로 **시작하는 모든** 파일을 찾아 한 번에 첨부합니다(확장자별로 여러 개 있어도 전부 불러옴). 지원 확장자: jpg, jpeg, gif, png, doc, docx, xls, xlsx, ppt, pptx, pdf.
- 활동을 등록하면 위 Photos/Bank/Receipts/Expenses(YYYY-MM/WeekN)와 Plan 폴더가 없을 경우 자동으로 생성됩니다.

### 회원 자동불러오기 (`assets/`)

Settings에서 형식을 TXT로 두면 `assets\members.txt`(탭으로 구분, 엑셀의 "텍스트(탭으로 구분)" 저장 형식과 동일), JSON으로 두면 `assets\members.json`을 회원 관리 화면의 "자동불러오기" 버튼이 읽습니다. Settings의 "회원 자동불러오기 파일 경로"를 지정하면 그 경로를 대신 읽습니다. 열 순서: 이름, Knox ID, 부서, 연락처, 가입 날짜, 회원 등급, 역할, 비고.

## 저장소에 포함된 예시 데이터

- `data/runtime/` — 샘플 회원·활동·게시판 데이터 (앱이 그대로 읽고 씁니다)
- `assets/` — 클럽 로고, 회원 자동불러오기용 샘플 `members.txt`
- `Input/` — Settings의 "데이터 루트 폴더"로 지정해 테스트해 본 샘플 사진/통장/영수증/경비/계획서 폴더
- `Report/` — 월간 정리 Excel 내보내기 결과 예시
- `screenshot/` — 앱 화면 스크린샷
- `backup/` — "DB 백업"으로 생성된 zip이 저장되는 폴더 (`.gitignore`에 포함되어 커밋되지 않음, 최초 백업 시 자동 생성)

## 주요 명령어

```bash
npm run dev      # Vite 개발 서버만 (브라우저)
npm start        # Electron 메인 빌드 + Vite 개발 서버 + Electron 창 실행
npm run build    # 프로덕션 빌드 (tsc -b && vite build && tsc -p tsconfig.node.json)
```

## 프로젝트 구조

자세한 아키텍처와 개발 규칙은 [`CLAUDE.md`](CLAUDE.md)를 참고하세요.
