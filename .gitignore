node_modules/
dist/
dist-electron/
*.log
.env
*.tsbuildinfo

# Local test data root (Photos/Receipts/Expenses/Plan) configured via Settings -
# points at an external, user-specific folder, not part of the repo.
Input/
